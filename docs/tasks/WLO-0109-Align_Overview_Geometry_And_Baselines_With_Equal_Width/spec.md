Measure mock and Android at equal logical viewport widths before geometry changes. Preserve WLO-0108 typography. Match reference spacing below hero (5dp), before summary (23dp), before summary divider (18dp), after divider (19dp), preserving standard M3 controls, their touch targets, and scaling. Produce equal-width comparison with matching data, distinguish CSS text boxes from Android accessibility bounds, and explicitly account for system bars and screenshot scale. Do not narrow the app based on a differently scaled screenshot. Verify build/static checks and visual rendering at normal/200% text.
