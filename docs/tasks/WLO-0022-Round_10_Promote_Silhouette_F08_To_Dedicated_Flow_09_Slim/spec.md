## Execution record (2026-09-12)

**The split.** Flow 04's Archive half (wireframes 5–8, mocks 5–8: lock
gate, capture ritual, delta card, timeline) moved verbatim into new
`09-silhouette-f08.html`, pins renumbered 14–25 → 1–12. Flow 04 slimmed to
pure F06 (4+4 phones): retitled, meta now F06-only, F08 styles moved out,
mock-4 captions re-pointed at flow 09, self-review rewritten to F06 items
with an explicit split note.

**First-class promotion (new in flow 09).**
- Wireframe 5: the capture reminder — generic copy, deep link to the gate,
  guilt-free "Not this week" (silent reschedule, F10 budget, R-U13).
- Mock 5 (pins 13–15): compare studio — wipe divider with centerline snap,
  synchronized scrubbing (per-pair detents), onion-skin toggle, angle tabs,
  comparability chip ("distance within 10 %"; non-comparable states say why).
- Mock 6 (pins 16–18): width profiles — per-band deltas (within-noise stays
  grey), weekly waist line with noise band, the "outlines agree with the
  tape" paired view; provenance trio measured/derived/estimated badged and
  never merged.
- Mock 7 (pins 19–21): capture detail sheet — flags name reason +
  consequence ("blur — excluded from delta claims"), reference is
  user-set only, delete shreds outline + profile + derived rows
  irreversibly (no photo exists to delete).
- Self-review rewritten for flow 09 (R-U16 re-checked across seven mocks,
  spec-coverage list incl. both fallbacks and data-quality gates, open
  items: derivation budget, comparability normalization [v1.x], R-B9).

**Cross-links.** index.html rows 04/09 (+ persona note), KICKOFF item 4
amended + item 9 added. Flow 01/06 buttons into flow 04 verified as
weigh-in links (still correct).

**Verification.** Leak scan 56 phones / 0 hits (one 'detent' leak in the
new compare mock caught and fixed). Browser: flow 09 measured 5 wireframes
+ 7 mocks with pins 1–21 in order; compare studio, profiles, and detail
sheet captured and inspected (pins 19–21 moved onto the sheet, verified
geometrically); flow 04 re-rendered and inspected (title, F06-only meta,
4+4 phones, no style fallout).
