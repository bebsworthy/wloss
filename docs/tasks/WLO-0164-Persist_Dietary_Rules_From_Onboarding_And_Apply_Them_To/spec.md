Persist the dietary rules already collected by F01 and consume them in F03's deterministic suggestions. Found during WLO-0156 verification: DietTemplateApplier produces FoodRules, but FinishOnboarding writes Targets and safety inputs only, then deletes the onboarding draft. There is currently no durable published DietPlan/PreferenceProfile reader for MealAgendaRepository.

Implement a versioned, profile-scoped document/repository for FoodRules (allergies, exclusions, dislikes and applicable diet tags). Publish through the F01 finish/studio path, keep previous preferences on failed writes, include the document in encrypted backup/restore, and resolve preferences when previewing suggestions. Pass hard rules to PlannerEngine.Constraints; do not infer lost legacy preferences or claim historical profiles are allergy-filtered. Commit should detect changed rules just as it detects changed targets. Existing manual meal planning must remain usable.

Verify onboarding→restart→suggestions, hard exclusion of matching recipe tags/ingredients, soft dislikes, changed-rule conflict, missing/corrupt rule handling, profile isolation and old/new backup round-trips. No new mandatory setup or meal agenda captions; any preference-management UX belongs in the existing F01 workflow.
